[package]
name = "testutils"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tokio = { version = "1.39", features = ["process", "io-util", "rt", "time"] }
assert_cmd = "2.0"
tempfile = "3"

